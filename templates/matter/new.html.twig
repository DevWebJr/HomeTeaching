{% extends 'layouts/base.html.twig' %}

{% block title 'New Matter | ' ~ parent() %}

{% block body %}
    <h1>Create new Matter</h1>

    {{ include('matter/_form.html.twig') }}

    <a href="{{ path('matter_index') }}">back to list</a>
{% endblock %}
