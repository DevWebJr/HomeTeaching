<!DOCTYPE html>

<title>Edit TextBook</title>

{% block body %}
    <h1>Edit TextBook</h1>

    {{ include('text_book/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('text_book_index') }}">back to list</a>

    {{ include('text_book/_delete_form.html.twig') }}
{% endblock %}
